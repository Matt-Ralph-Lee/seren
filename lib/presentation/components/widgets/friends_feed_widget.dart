import 'package:flutter/material.dart';

class FriendsFeedWidget extends StatelessWidget {
  const FriendsFeedWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text(
      "friends feed",
      style: TextStyle(color: Colors.white),
    );
  }
}
